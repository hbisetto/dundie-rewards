# pytest.toml or .pytest.toml
[tool.pytest.ini_options]
minversion = "9.0"
addopts = ["-ra", "-q", "-vv"]
testpaths = ["tests","integration"]

[tool.flake8]
exclude= [".venv", "build"]
max-line-length = 79

