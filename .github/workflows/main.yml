name: CI

on:
  push:
    branches:
      - "main"
  pull_request:
    branches:
      - "main"

  workflow_dispatch:

jobs:
  lint:
    strategy:
          fail-fast: true
          matrix:
            python-version: ['3.10'] # teste de versões
            os: [ubuntu-latest]
    runs-on: ${{matrix.os}}
    steps: 
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v4
        with:
          python-version: ${{matrix.python-version}}
      - name: Install Project    
        run: pip install '.[test]'
      - name: Look for style errors
        run: pflake8

  tests:
    needs: lint
    strategy:
      fail-fast: false
      matrix:
        python-version: ['3.8', '3.9', '3.10'] # teste de versões
        os: [ubuntu-latest]
    runs-on: ${{matrix.os}}
    permissions:
      checks: write
      pull-requests: write
      contents: read
    steps: 
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v4
        with:
          python-version: ${{matrix.python-version}}
      - name: Install Project    
        run: |
          python -m pip install --upgrade pip
          python -m pip uninstall -y pytest 
          pip install --upgrade '.[test]'
      - name: Run tests
        run: pytest -v --junitxml=test-result.xml 
      - name: publish junit results
        uses: EnricoMi/publish-unit-test-result-action@v1
        if: always()
        with:
          files: test-result.xml
          check_name: Test Result (Python ${{matrix.python-version}})
